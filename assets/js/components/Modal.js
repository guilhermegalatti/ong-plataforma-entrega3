export function ensureModal(){let b=document.getElementById('backdrop-global');if(!b){b=document.createElement('div');b.id='backdrop-global';b.className='modal-backdrop';b.innerHTML='<div class="modal"><h3 id="modal-title">Informação</h3><p id="modal-content">...</p><button class="btn btn-primary" id="modal-close">Fechar</button></div>';document.body.appendChild(b);b.addEventListener('click',e=>{if(e.target===b)b.classList.remove('open')});b.querySelector('#modal-close').addEventListener('click',()=>b.classList.remove('open'))}return b}
export function openModal(title,content){const b=ensureModal();b.querySelector('#modal-title').textContent=title||'Informação';b.querySelector('#modal-content').textContent=content||'';b.classList.add('open')}